apps:
  - script : ./bin/www
    name: 'hackonload.com'
    watch: true
    instances: max
    exec_mode: cluster
    ignore_watch: ["node_modules"]

  env :
    NODE_ENV: development
  env_production:
    NODE_ENV: production
